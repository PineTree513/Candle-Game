using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MenuDataStatic : MonoBehaviour
{
    //public static SaveData[] files = new SaveData[] { new SaveData(), new SaveData(), new SaveData()};
    public static SaveData[] files = new SaveData[] {null, null, null};

    public static string targetSaveName;
}
